{
  "name": "anugadget-aws-cdk",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -w -p tsconfig.json",
    "bootstrap": "cdk bootstrap",
    "deploy": "cdk deploy --all",
    "synth": "cdk synth"
  },
  "devDependencies": {
    "aws-cdk": "2.x",
    "typescript": "5.x"
  },
  "dependencies": {
    "aws-cdk-lib": "2.x",
    "constructs": "10.x"
  }
}
